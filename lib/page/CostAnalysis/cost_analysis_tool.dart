import 'package:flutter/material.dart';

class CostAnalysis extends StatelessWidget {
  const CostAnalysis();

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Container());
  }
}